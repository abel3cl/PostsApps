import Foundation

enum Constants {
    enum Posts {
        static let path = "posts"
    }

    enum User {
        static let path = "users"
    }

    enum Comments {
        static let path = "comments"
        static let postId = "postId"
    }
}
