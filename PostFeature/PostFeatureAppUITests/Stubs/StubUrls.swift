import Foundation

enum StubUrls {
    enum Post {
        static let get = "/posts"
    }
    enum Comment {
        static let get = "/comments"
    }
    enum User {
        static let get = "/users/1"
    }
}
